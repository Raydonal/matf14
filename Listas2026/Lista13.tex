\documentclass[11pt]{article}
\newcommand{\ListaTitulo}{Lista 13: Simulado Prova 2 (Unidades 2 e 3)}
\input{preamble}

\begin{document}

\cabecalho{Lista de Exercícios 13: Simulado}{Revisão geral, no formato e no nível da Prova 2: Probabilidade e VA discretas (Cap. 2--3, Aulas 09--23)}

\noindent \textit{Simulado de 10 pontos, no mesmo formato da prova oficial. Resolva com tempo
cronometrado (sugestão: 100 minutos, sem consulta) antes de olhar o gabarito em
\texttt{Gabarito13.pdf}.}

\begin{enumerate}

\item Considere uma variável aleatória discreta $X$ com distribuição de probabilidade:
\begin{center}
\begin{tabular}{c|ccccc}
\toprule
$k$ & 1 & 2 & 3 & 4 & 5 \\
\midrule
$P(X=k)$ & $2/10$ & $1/10$ & $3/10$ & $2/10$ & $2/10$ \\
\bottomrule
\end{tabular}
\end{center}
\begin{enumerate}
  \item (1 ponto) Calcule $P(|X-3|\ge 2)$.
  \item (1 ponto) Calcule $E(X)$ e $\mathrm{Var}(X)$.
  \item (1 ponto) Calcule a função de distribuição acumulada de $X$.
\end{enumerate}

\item Uma linha de produção tem 2\% de peças defeituosas, de forma independente entre peças.
Deseja-se a probabilidade de que uma amostra de 150 peças contenha no máximo 3 defeituosas.
\begin{enumerate}
  \item (1 ponto) Calcule essa probabilidade usando a distribuição Binomial exata.
  \item (1 ponto) Calcule a mesma probabilidade usando a aproximação pela distribuição Poisson.
\end{enumerate}

\item Uma financeira tem 600 clientes classificados por faixa de renda e situação de pagamento:

\begin{center}
\begin{tabular}{l|cc|c}
\toprule
 & Em dia & Inadimplente & Total \\
\midrule
Renda baixa & 180 & 60 & 240 \\
Renda alta  & 320 & 40 & 360 \\
\midrule
Total       & 500 & 100 & 600 \\
\bottomrule
\end{tabular}
\end{center}

\begin{enumerate}
  \item (1 ponto) Qual é a probabilidade de um cliente sorteado estar inadimplente?
  \item (1 ponto) Qual é a probabilidade de um cliente sorteado estar inadimplente, dado que é de
    renda baixa?
  \item (1 ponto) Situação de pagamento e faixa de renda são independentes nesta base? Justifique
    numericamente.
\end{enumerate}

\item Um exame laboratorial para uma doença tem 95\% de sensibilidade (detecta a doença quando ela
existe) e 92\% de especificidade (dá negativo quando o paciente é saudável). A prevalência da
doença na população testada é de 1\%.
\begin{enumerate}
  \item (1 ponto) Calcule a probabilidade de um paciente sorteado ao acaso testar positivo.
  \item (1 ponto) Dado que o teste deu positivo, calcule a probabilidade de o paciente
    verdadeiramente ter a doença.
\end{enumerate}

\end{enumerate}

\vspace{1cm}
\begin{center}
\Large\textbf{Formulário}
\end{center}
\begin{small}
\begin{itemize}
  \item Se $X$ é discreta, $E(X)=\sum_k k\,P(X=k)$; $\mathrm{Var}(X)=E(X^2)-[E(X)]^2$;
    $dp(X)=\sqrt{\mathrm{Var}(X)}$.
  \item $X\sim\text{Bin}(n,p)$: $P(X=k)=\binom{n}{k}p^k(1-p)^{n-k}$; $E(X)=np$,
    $\mathrm{Var}(X)=np(1-p)$.
  \item $X\sim\text{Pois}(\lambda)$: $P(X=k)=e^{-\lambda}\lambda^k/k!$; $E(X)=\mathrm{Var}(X)=\lambda$;
    aproxima Bin$(n,p)$ com $\lambda=np$.
  \item $P(A\cup B)=P(A)+P(B)-P(A\cap B)$; $P(B\mid A)=P(A\cap B)/P(A)$, se $P(A)>0$.
    Independência: $P(A\cap B)=P(A)P(B)$.
  \item Prob. total: $P(A)=\sum_i P(A\mid B_i)P(B_i)$. Bayes: $P(B_i\mid A)=\dfrac{P(A\mid
    B_i)P(B_i)}{\sum_j P(A\mid B_j)P(B_j)}$.
\end{itemize}
\end{small}

\end{document}
